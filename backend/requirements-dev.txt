# Dependencias de desarrollo y CI.
# Incluye las de runtime, así `pip install -r requirements-dev.txt` alcanza para todo.
-r requirements.txt

pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.0
httpx==0.28.1  # requerido por fastapi.testclient.TestClient
